var socket = io();

socket.on('testEvent', function(data){
    
})






// -ENTRY: when player clicks shared-link, add then to lobby (add player)
// -when added, assign random avatar (assign player)
// -when play game button hit, begin game/show qs (play game)

// -when game starts, show player their avatar info (show avatar)
// -when avatar is done being shown, continue on to player listening for new question containing responses (show question)
// -when selecting response, record player responses (player response)